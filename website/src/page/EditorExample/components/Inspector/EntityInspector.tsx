import { Entity } from 'deer-engine';
import { FC } from 'react';

interface EntityInspectorProps {
  entity: Entity;
}

export const EntityInspector: FC<EntityInspectorProps> = (props) => {
  const {} = props;

  return <div>EntityInspector</div>;
};
